## Packages
qrcode.react | Renders the WhatsApp authentication QR Code as an SVG/Canvas
date-fns | Human-readable date formatting for chat logs

## Notes
Polling /api/whatsapp/status every 3 seconds to update connection status dynamically
